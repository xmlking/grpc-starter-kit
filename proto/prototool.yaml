# keep this file at proto/prototool.yaml

# Paths to exclude when searching for Protobuf files.
excludes:
  - e2e
  - build
  - dist
  - node_modules

# Protoc directives.
protoc:
  version: 3.13.0
  includes:
    - ../third_party/proto
